import React from 'react';
import tw from 'twin.macro';
import { Link } from 'gatsby';
import Navigation from './Navigation';
import logo from '../../logo-condensed.svg';

const Header = () => (
  <header>
    <Navigation />
  </header>
);
export default Header;
